# syntax=docker/dockerfile:1

FROM node:12.18.1

ENV NODE_ENV=production

ENV host=database

ENV PORT=3002

COPY . ./app

WORKDIR /app

EXPOSE 3002

RUN npm install

ENTRYPOINT ["/bin/sh", "-c" , "npm run migrate && npm start"]